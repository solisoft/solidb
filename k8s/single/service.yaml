apiVersion: v1
kind: Service
metadata:
  name: solidb
  namespace: solidb
  labels:
    app.kubernetes.io/name: solidb
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 6745
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: solidb
